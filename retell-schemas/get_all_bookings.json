{
  "type": "object",
  "properties": {
    "action": {
      "type": "string",
      "const": "get_all_bookings",
      "description": "Fixed action identifier for routing"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Customer's email address to search for bookings. At least one of email or phoneNumber must be provided."
    },
    "phoneNumber": {
      "type": "string",
      "pattern": "^\\+[1-9]\\d{1,14}$",
      "description": "Customer's phone number in E164 format (e.g., +15551234567). At least one of email or phoneNumber must be provided."
    }
  },
  "required": ["action"]
}
